version: '3.8'
services:
  app-springboot:
    image: ${REGISTRY_IP}:5000/app-springboot:latest
    ports:
      - "80:8080"
    restart: always
    environment:
      - SERVER_PORT=8080
    volumes:
      - ./app_data:/app/data
      - ./logs:/tmp/logs
