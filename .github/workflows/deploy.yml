name: CD Deploy

on:
  push:
    branches:
      - main
      - develop

jobs:
  deploy:
    name: Deploy to ${{ github.ref_name == 'main' && 'Production' || 'Development' }}
    runs-on: [self-hosted, "${{ github.ref_name == 'main' && 'prod' || 'dev' }}"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy with Docker Compose
        run: |
          echo "Haciendo pull de la última imagen desde el Registry local..."
          # Usamos el docker-compose que está en el repo para este despliegue
          docker compose pull
          
          echo "Levantando servicios..."
          docker compose up -d
